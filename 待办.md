# Bili Digest 项目状态

> 唯一状态入口。会话恢复先读本文件。

## 项目信息

| 项 | 值 |
| --- | --- |
| 位置 | `D:\00-projects\Github\bili-digest` |
| 定位 | B站版 YouTube Digest，个人学习用 Chrome 扩展 |
| 目标 | 第一个开源项目；锻炼全栈 / 工程能力，丰富简历 |
| 许可 | MIT |

## 已完成

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| WBI 签名 | ✅ | `lib/wbi.js`，与官方样例一致，有单测 |
| 字幕获取 | ✅ | `x/player/wbi/v2` + 字幕 JSON 解析（需登录态） |
| 侧边栏 UI | ✅ | 字幕 / 概览 / 笔记 / 对话 / 设置 五个页签 |
| AI 功能 | ✅ | 翻译、概览、逐句解释 |
| AI 接口 | ✅ | 单一「OpenAI 兼容」入口（Base URL / 模型 / Key 自填），按地址自动识别 DeepSeek 与 Anthropic 特殊参数，旧设置自动迁移 |
| 导出 Markdown | ✅ | `lib/export.js`：概览 + 双语字幕 + 笔记一键导出，有单测 |
| 快捷标记 | ✅ | 悬浮「标记」按钮 + N 快捷键，直接保存当前句原文为带时间戳标记，不调用 AI |
| 笔记增强 | ✅ | 复制文本 / 复制时间戳 / 一键回看；支持「本视频 / 全部视频」视图 |
| 复制全文与简介 | ✅ | 字幕一键复制；导出带视频简介 |
| 概览金句 | ✅ | 分析 prompt 增加 key_quotes，可复制 / 一键存为笔记 |
| 笔记 AI 润色 | ✅ | `polishNote` + `prompts/polish.md`，保存前可润色草稿 |
| 笔记 | ✅ | 本地保存、时间戳跳转、删除 |
| 文档 | ✅ | README / PRIVACY / LICENSE / prompts |
| 测试与检查 | ✅ | `npm test`、`npm run check`、打包脚本 |
| 提取链路加固 | ✅ | 参考 Clipper：浏览器头 + Referer，aid+cid 主来源，player/v2 回退 |
| 界面重构 | ✅ | B站配色（蓝 `#00AEEC` / 粉 `#FB7299`）+ 日间/夜间主题切换；本地打包字体（Stack Sans 拉丁 + MiSans 中文三字重，Apache 2.0） |
| 视频对话 | ✅ | 基于字幕问答（流式），按视频本地保存，可导出 Markdown |

## 待办

- [ ] 在 Chrome 实测：登录 B站后验证字幕抓取、翻译、跳转
- [ ] 截图放到 `screenshots/` 并补充 README
- [ ] 推送到 GitHub（本地已 git init；推送前确认 git 用户名邮箱，建议用 GitHub 账号邮箱）
- [ ] 多 P 视频分 P 切换
- [ ] 字幕跟随播放自动滚动
- [ ] 字幕内搜索
- [ ] 笔记导出 Anki / CSV

## 关键文件速查

| 文件 | 作用 |
| --- | --- |
| `background.js` | 后台服务：B站接口、所选 AI 供应商、缓存、笔记 |
| `content.js` | 内容脚本：按钮注入、视频上下文、跳转 |
| `sidepanel.*` | 侧边栏界面 |
| `options.*` | 设置页 |
| `lib/wbi.js` | WBI 签名 |
| `lib/subtitle.js` | 字幕解析 |
| `lib/ai.js` | 多 AI 供应商统一客户端 |
| `lib/export.js` | Markdown 导出组装（纯函数，有单测） |
| `lib/note-context.js` | 按播放时间从字幕取当前句上下文（纯函数，有单测） |
| `prompts/*.md` | AI 提示词模板（含视频问答 `chat.md`） |

## 已验证的事实（勿臆改）

- B站字幕接口：`https://api.bilibili.com/x/player/wbi/v2?aid=&cid=&bvid=`（无需签名即可用，失败回退 `player/v2`），**未登录时字幕列表为空**；
- B站接口请求需带浏览器头、`zh-CN` 语言头、`Cache-Control: no-cache` 和 `referrer=https://www.bilibili.com/`，否则易被 412 拦截；
- `x/web-interface/view` 当前必须 WBI 签名（未签名返回 412），扩展内用签名版获取 aid/cid；
- 页面 SSR HTML 里**没有** `__playinfo__`（由播放器运行时注入），取 cid 应走 `__INITIAL_STATE__`（`videoData.cid` / `state.cid` / `pages[].cid`），或让后台用 view 接口兜底；
- 字幕 JSON：`body[].{from,to,location,content}`，时间单位秒；
- DeepSeek：Base `https://api.deepseek.com`，模型 `deepseek-v4-flash`，非思考模式 `thinking:{type:"disabled"}`（deepseek-chat/reasoner 已于 2026-07-24 弃用）；
- Anthropic 官方提供 OpenAI SDK 兼容端点：`https://api.anthropic.com/v1/`（官方标注测试用途），Claude Key 当 Bearer Key 用即可；扩展对其不加 `response_format`（靠 prompt 约束 + 容错解析），并附加 `anthropic-version: 2023-06-01` 头。
- MV3 Service Worker 会被浏览器随时回收，**流式长连接端口不可靠**；对话流式已改为在侧边栏页面直连 AI 接口（字幕就在页面内存里），后台不再参与对话转发。
- B站页面整体是 Vue SSR（`#app` 含顶部导航与视频区）：**内容脚本严禁向 B站自己管理的 DOM 插节点**，否则 hydration 冲突会导致整页重渲染、顶部导航消失；「精读」按钮必须挂在 body 下的独立宿主上做 fixed 定位（见 `content.js`）。
